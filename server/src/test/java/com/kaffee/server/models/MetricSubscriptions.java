package com.kaffee.server.models;

public class MetricSubscriptions {
  // verify that we add subscriptions to the subscriptions list

  // verify that we cannot add an invalid subscription to the list

  // verify that we do not add duplicate subscriptions

  // verify that we can remove subscriptions from the list

  // verify that if we attempt to remove an invalid subscription, we have no error

  // verify that we can change the jmx port

  // verify that we can change the kafka url

  // verify that we can change the kafka port
}
